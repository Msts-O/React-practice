const initState = {}
const writerReducer = (state = initState, action) => {
    return state
}

export default authReducerconst initState = {}
const authReducer = (state = initState, action) => {
    return state
}

export default authReducer